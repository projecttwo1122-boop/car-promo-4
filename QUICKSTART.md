# Car Promo 4 - Quickstart Guide

## 📋 Требования

- Node.js >= 18.0.0
- npm >= 9.0.0

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd /Users/maksnalyvaiko/personal/cars/car-promo-4
npm install
```

### 2. Создание файла окружения

Создайте файл `.env.local` в корне проекта:

```bash
NEXT_PUBLIC_API_URL=http://localhost:3000
```

Для production:
```
NEXT_PUBLIC_API_URL=https://your-api.railway.app
```

### 3. Запуск development сервера

```bash
npm run dev
```

Откройте [http://localhost:3004](http://localhost:3004) в браузере.

## 🏗️ Production Build

```bash
npm run build
npm start
```

## 📦 Структура проекта

```
car-promo-4/
├── src/
│   ├── app/
│   │   ├── page.tsx          # Главная страница
│   │   ├── layout.tsx        # Root layout
│   │   └── layout.css        # Layout стили
│   ├── components/
│   │   ├── Header.tsx        # Навигация
│   │   ├── Hero.tsx          # Hero секция
│   │   ├── CarsShowcase.tsx  # Галерея авто
│   │   ├── CarCard.tsx       # Карточка авто
│   │   ├── Stats.tsx         # Статистика
│   │   ├── Features.tsx      # Преимущества
│   │   └── Footer.tsx        # Подвал
│   ├── lib/
│   │   └── api.ts            # API клиент
│   └── styles/
│       ├── globals.css       # Глобальные стили
│       ├── Hero.module.css
│       ├── Header.module.css
│       ├── CarsShowcase.module.css
│       ├── CarCard.module.css
│       ├── Stats.module.css
│       ├── Features.module.css
│       └── Footer.module.css
├── public/                   # Статические файлы
├── package.json
├── tsconfig.json
├── next.config.js
└── README.md
```

## 🎨 Ключевые особенности

### Компоненты

1. **Header** - Липкая навигация с эффектом blur при скролле
2. **Hero** - Анимированный hero с градиентами и плавающими элементами
3. **CarsShowcase** - Сетка с карточками автомобилей
4. **CarCard** - Индивидуальная карточка с изображением и спецификациями
5. **Stats** - Анимированные счётчики статистики
6. **Features** - Сетка преимуществ с иконками
7. **Footer** - Многоколончный подвал с ссылками

### Анимации

- Gradient animations
- Floating elements
- Hover effects на карточках
- Scroll-triggered animations
- Counter animations для статистики
- Smooth transitions и микро-взаимодействия

## 🌐 API интеграция

Приложение получает данные из `car-api`:

```typescript
// src/lib/api.ts
const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3000';

export const getCars = async (params?: { limit?: number }) => {
  const response = await fetch(`${API_URL}/car?limit=${params?.limit || 12}`);
  return response.json();
};
```

## 🎯 Customization

### Изменение цветов

Отредактируйте CSS переменные в `src/styles/globals.css`:

```css
html {
  --primary-color: #FF6B35;      /* Оранжевый */
  --secondary-color: #004E89;    /* Тёмно-синий */
  --accent-color: #00D9FF;       /* Голубой */
  --dark-bg: #000000;            /* Чёрный фон */
  --surface: #111111;            /* Поверхность */
}
```

### Изменение текста

Все текстовые строки находятся в компонентах. Отредактируйте необходимые файлы в `src/components/`.

### Изменение количества карточек

В `src/components/CarsShowcase.tsx`:

```typescript
const fetchedCars = await getCars({ limit: 12 }); // Измените число
```

## 🚢 Развёртывание

### Railway

1. Push код на Git
2. Подключите репозиторий к Railway
3. Установите environment переменные
4. Deploy!

### Vercel

```bash
vercel deploy
```

### Docker

```bash
docker build -t car-promo-4 .
docker run -p 3004:3004 car-promo-4
```

## ⚡ Performance Tips

- Изображения загружаются лениво
- CSS модули ограничивают scope стилей
- Animations используют GPU-accelerated свойства
- Framer Motion оптимизирует renderng

## 🐛 Troubleshooting

### Изображения не загружаются

Проверьте `NEXT_PUBLIC_API_URL` в `.env.local` и убедитесь, что API доступен.

### Анимации не работают

Проверьте поддержку браузером Framer Motion (Chrome, Firefox, Safari последних версий).

### Стили не применяются

Очистите кэш:

```bash
npm run build
rm -rf .next
npm run dev
```

## 📚 Дополнительные ресурсы

- [Next.js документация](https://nextjs.org/docs)
- [Framer Motion документация](https://www.framer.com/motion/)
- [TypeScript документация](https://www.typescriptlang.org/docs/)

## 💡 Советы для разработки

1. Используйте React Developer Tools расширение
2. Проверяйте производительность в Chrome DevTools
3. Тестируйте на мобильных устройствах
4. Используйте lighthouse для аудита

## 📝 Лицензия

Private project

---

Создано с ❤️ для premium автомобилей



